export enum QrcodeStatusEnum {
  active = 'active',
  expired = 'expired',
  loading = 'loading'
}

export enum PLAY_MODE {
  SEQUENCE_MODE = 'SEQUENCE_MODE', // 顺序播放
  LOOP_MODE = 'LOOP_MODE', // 循环播放
  SINGLE_LOOP_MODE = 'SINGLE_LOOP_MODE', // 单曲循环
  RANDOM_MODE = 'RANDOM_MODE' //随机
}
